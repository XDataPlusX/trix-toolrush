name: trix-toolrush
version: 0.1.0
description: "Trix ToolRush — Linux/Docker acceleration lanes for Hermes Agent (native rg search transport, batched parallel read RPC, streaming warm shell). Linux port of ToolRush v2 (github.com/OnlyTerp/toolrush). In-memory function-level compatibility patches, fail-closed per lane. Kill-switches: plugins.enabled removal, config trix-toolrush.enabled=false, env TRIX_TOOLRUSH_*=0 / TOOLRUSH_*=0."
author: XDataPlus
hooks: []
